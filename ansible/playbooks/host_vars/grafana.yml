---
# PLACEHOLDER — substitua e criptografe com:
#   ansible-vault encrypt host_vars/grafana.yml
grafana_admin_user: admin
grafana_admin_password: "CHANGE_ME_ADMIN_PASSWORD"
grafana_root_url: "http://192.168.1.130:3000"

firewall_allowed_ports:
  - { port: 3000, proto: tcp, from: "{{ firewall_lan_cidr }}" }
